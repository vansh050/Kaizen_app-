<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- The Daily/WebRTC voice library (used AUDIO-ONLY here) transitively
         declares FOREGROUND_SERVICE_MEDIA_PROJECTION + a MediaProjectionService
         for screen-sharing, which this app never does. Strip both so the merged
         manifest carries no media-projection capability and Play Console does
         not require the FOREGROUND_SERVICE_MEDIA_PROJECTION declaration. Safe:
         the MediaProjectionService is only started on screen-share, never on
         audio calls. -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"
        tools:node="remove" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <meta-data
            android:name="cashfree_subscription_flow_enable"
            android:value="true"
            tools:replace="android:value" />

        <meta-data
            android:name="com.dieam.reactnativepushnotification.notification_foreground"
            android:value="true" />

        <!-- Remove the WebRTC screen-share foreground service (audio-only app). -->
        <service
            android:name="com.oney.WebRTCModule.MediaProjectionService"
            tools:node="remove" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="test.alphaquark.in"
                    android:pathPrefix="/subscriptions" />
            </intent-filter>

            <!--
                Campaign smart links (WhatsApp/SMS/email) via
                app-links.alphaquark.in/l/<tenant>?utm_*&dl=... — opens
                directly in the app when installed (verified App Link, no
                chooser). The backend SmartLinkRouter renders its
                interstitial / Play-Store fallback only when the app is
                NOT installed. Parsed in App.js → src/utils/smartLink.js.
                Verification via /.well-known/assetlinks.json served by
                aq_backend_github WellKnownRouter — that file must list
                Kaizen's applicationId (com.aq.kaizenalpha) + signing
                fingerprint before autoVerify actually verifies.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="app-links.alphaquark.in" />
                <data android:pathPrefix="/l" />
            </intent-filter>

            <!-- Custom scheme matches REACT_APP_DEEP_LINK_SCHEME in .env.
                 Consumed by PayUService (payment return URL) and
                 ZerodhaOAuthService (OAuth WebView close trigger). Without
                 this filter the OS has no app to hand off kaizenapp:// URLs,
                 so payment success and Zerodha login silently fail to
                 return to the app. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kaizenapp" />
            </intent-filter>
        </activity>
    </application>
</manifest>
