diff --git a/node_modules/react-native-version-check/node_modules/semver/semver.js b/node_modules/react-native-version-check/node_modules/semver/semver.js
index 39319c1..4d3aa01 100644
--- a/node_modules/react-native-version-check/node_modules/semver/semver.js
+++ b/node_modules/react-native-version-check/node_modules/semver/semver.js
@@ -19,27 +19,27 @@ if (typeof process === 'object' &&
 // Not necessarily the package version of this code.
 exports.SEMVER_SPEC_VERSION = '2.0.0'
 
-var MAX_LENGTH = 256
-var MAX_SAFE_INTEGER = Number.MAX_SAFE_INTEGER ||
+let MAX_LENGTH = 256
+let MAX_SAFE_INTEGER = Number.MAX_SAFE_INTEGER ||
   /* istanbul ignore next */ 9007199254740991
 
 // Max safe segment length for coercion.
-var MAX_SAFE_COMPONENT_LENGTH = 16
+let MAX_SAFE_COMPONENT_LENGTH = 16
 
-var MAX_SAFE_BUILD_LENGTH = MAX_LENGTH - 6
+let MAX_SAFE_BUILD_LENGTH = MAX_LENGTH - 6
 
 // The actual regexps go on exports.re
-var re = exports.re = []
-var safeRe = exports.safeRe = []
-var src = exports.src = []
-var t = exports.tokens = {}
-var R = 0
+let re = exports.re = []
+let safeRe = exports.safeRe = []
+let src = exports.src = []
+let t = exports.tokens = {}
+let R = 0
 
 function tok (n) {
   t[n] = R++
 }
 
-var LETTERDASHNUMBER = '[a-zA-Z0-9-]'
+let LETTERDASHNUMBER = '[a-zA-Z0-9-]'
 
 // Replace some greedy regex tokens to prevent regex dos issues. These regex are
 // used internally via the safeRe object since all inputs in this library get
@@ -47,7 +47,7 @@ var LETTERDASHNUMBER = '[a-zA-Z0-9-]'
 // regexes are exported for userland consumption and lower level usage. A
 // future breaking change could export the safer regex only with a note that
 // all input should have extra whitespace removed.
-var safeRegexReplacements = [
+let safeRegexReplacements = [
   ['\\s', 1],
   ['\\d', MAX_LENGTH],
   [LETTERDASHNUMBER, MAX_SAFE_BUILD_LENGTH],
@@ -213,7 +213,7 @@ tok('TILDETRIM')
 src[t.TILDETRIM] = '(\\s*)' + src[t.LONETILDE] + '\\s+'
 re[t.TILDETRIM] = new RegExp(src[t.TILDETRIM], 'g')
 safeRe[t.TILDETRIM] = new RegExp(makeSafeRe(src[t.TILDETRIM]), 'g')
-var tildeTrimReplace = '$1~'
+let tildeTrimReplace = '$1~'
 
 tok('TILDE')
 src[t.TILDE] = '^' + src[t.LONETILDE] + src[t.XRANGEPLAIN] + '$'
@@ -229,7 +229,7 @@ tok('CARETTRIM')
 src[t.CARETTRIM] = '(\\s*)' + src[t.LONECARET] + '\\s+'
 re[t.CARETTRIM] = new RegExp(src[t.CARETTRIM], 'g')
 safeRe[t.CARETTRIM] = new RegExp(makeSafeRe(src[t.CARETTRIM]), 'g')
-var caretTrimReplace = '$1^'
+let caretTrimReplace = '$1^'
 
 tok('CARET')
 src[t.CARET] = '^' + src[t.LONECARET] + src[t.XRANGEPLAIN] + '$'
@@ -251,7 +251,7 @@ src[t.COMPARATORTRIM] = '(\\s*)' + src[t.GTLT] +
 // this one has to use the /g flag
 re[t.COMPARATORTRIM] = new RegExp(src[t.COMPARATORTRIM], 'g')
 safeRe[t.COMPARATORTRIM] = new RegExp(makeSafeRe(src[t.COMPARATORTRIM]), 'g')
-var comparatorTrimReplace = '$1$2$3'
+let comparatorTrimReplace = '$1$2$3'
 
 // Something like `1.2.3 - 1.2.4`
 // Note that these all use the loose form, because they'll be
@@ -645,7 +645,7 @@ function diff (version1, version2) {
 
 exports.compareIdentifiers = compareIdentifiers
 
-var numeric = /^[0-9]+$/
+let numeric = /^[0-9]+$/
 function compareIdentifiers (a, b) {
   var anum = numeric.test(a)
   var bnum = numeric.test(b)
@@ -826,7 +826,7 @@ function Comparator (comp, options) {
   debug('comp', this)
 }
 
-var ANY = {}
+let ANY = {}
 Comparator.prototype.parse = function (comp) {
   var r = this.options.loose ? safeRe[t.COMPARATORLOOSE] : safeRe[t.COMPARATOR]
   var m = comp.match(r)
